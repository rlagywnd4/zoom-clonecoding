doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
    body 
        header
            h1 Noom
        main
            form#nick
                input(type='text', placeholder='chose a nickname', required)
                button Send
            ul
            form#message
                input(type='text', placeholder='write a msg', required)
                button Send
        script(src='/public/js/app.js')